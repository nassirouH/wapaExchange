import SwiftUI

enum AppColors {
    static let brand = Color(red: 0.10, green: 0.42, blue: 0.95)
    static let brandDark = Color(red: 0.06, green: 0.28, blue: 0.70)
    static let accent = Color(red: 0.96, green: 0.65, blue: 0.14)

    static let background = Color(uiColor: .systemBackground)
    static let secondaryBackground = Color(uiColor: .secondarySystemBackground)
    static let tertiaryBackground = Color(uiColor: .tertiarySystemBackground)

    static let textPrimary = Color(uiColor: .label)
    static let textSecondary = Color(uiColor: .secondaryLabel)
    static let textTertiary = Color(uiColor: .tertiaryLabel)

    static let separator = Color(uiColor: .separator)
    static let success = Color(red: 0.16, green: 0.66, blue: 0.38)
    static let warning = Color(red: 0.97, green: 0.62, blue: 0.05)
    static let danger = Color(red: 0.89, green: 0.21, blue: 0.27)
}
